
<block wx:if="{{size=='small'}}" >
	<button open-type="{{openType||''}}" class="btn small"><slot></slot></button>
</block>
<block wx:else class="btn">
	<button open-type="{{openType||''}}" class="btn"><slot></slot></button>
</block>
